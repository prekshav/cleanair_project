name: cleanair_dashboard
description: "A new Flutter project for CleanAir & Clear Streets."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  google_maps_flutter: ^2.11.0 # Or your latest available version
  flutter_typeahead: ^5.2.0    # For the clean, medium auto-search bar
  http: ^1.2.0
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

  # THIS SECTION MUST BE AT THE BOTTOM AND NOT INDENTED UNDER DEPENDENCIES
flutter:
  uses-material-design: true